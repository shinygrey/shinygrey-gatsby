/*import { graphql } from "gatsby";

export const wpGraphHome = graphql`query WpGraph {
	allWpPage {
		edges {
			node {
			id
			slug
			title
			isFrontPage
			isPostsPage
			content
			}
		}
	}
	allWpPost(sort: { fields: [date] }) {
		nodes {
		title
		excerpt
		slug
		}
	}
}`;
*/